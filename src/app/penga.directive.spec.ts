import { PengaDirective } from './penga.directive';

describe('PengaDirective', () => {
  it('should create an instance', () => {
    const directive = new PengaDirective();
    expect(directive).toBeTruthy();
  });
});
